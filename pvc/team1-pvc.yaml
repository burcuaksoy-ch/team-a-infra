apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: team1-pvc
  namespace: team-1-prod
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
  storageClassName: microk8s-hostpath
